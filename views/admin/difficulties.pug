extends layout_admin

block content
    div.container
        h2 #{translation.DIFFICULTIES}

        table#tableDifficulties.highlight
            thead
                tr
                    th #{translation.NAME}
                    th.right #{translation.ACTION}

            tbody#bodyTracks
                tr
                    td.tdEditable(colspan='2')
                        form(method='post')
                            .input-field.inline
                                input#name.validate(maxlength='50', type='text', name='name', required='')
                                label(for='name') #{translation.NAME}
                            button.right.btn.waves-effect.waves-light(type='submit', name='submit') #{translation.ADD}
                                i.material-icons.left add
                    td
                each difficulty in difficulties
                    tr
                        .difficulty
                            td
                                span(id="diff"+difficulty.id)= difficulty.name
                            td
                                a(href="/admin/difficulties/remove/"+difficulty.id).right.black-text
                                    i.material-icons delete
                                a(onclick="editDiff('"+difficulty.id+"')").right.black-text
                                    i.material-icons mode_edit