extends layout_admin

block content
    div.container
        h2 #{translation.DIFFICULTIES}

        table#tableDifficulties.highlight
            thead
                tr
                    th #{translation.NAME}

            tbody
                tr
                    td.tdEditable(colspan='2')
                        form(method='post')
                        .input-field.inline
                            input#name.validate(maxlength='50', type='text', name='name', required='')
                            label(for='name') #{translation.NAME}
                        button.right.btn.waves-effect.waves-light(type='submit', name='submit')
                            | #{translation.ADD}
                    td
                each difficulty in difficulties
                    tr(onclick='getAll(' + difficulty.id + ')')
                        .difficulty
                            td
                                value=difficulty.name
                            td
                                button.right.btn.waves-effect.waves-light(onclick='delete(' + difficulty.id + ')') #{translation.DELETE}